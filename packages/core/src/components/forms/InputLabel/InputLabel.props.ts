export interface InputLabelProps {
  /**
   * The label text
   */
  label?: string;

  /**
   * Whether the field is required (shows asterisk)
   */
  required?: boolean;

  /**
   * Whether the field is disabled (changes color)
   */
  disabled?: boolean;
}
